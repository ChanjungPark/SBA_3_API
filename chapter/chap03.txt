nn ?

neuron vs nerual network ?

단수 , 복수

json : {} , []
java : Member List<Member>, 객체, 컬렉션

scalar vector


y = f(x) function
f(x) setter : consumer
y = f() getter : supplier
bool = f(x) : predicate
f() : runnable (x)

function + prop = object + object = collection

function + function = algorithm + algorithm = model

prop + prop = database


neu -- ron : 이진 출력 Y / N   0,1   bool 함수
percept -- ron : 인식하는 뉴런   확률 (가중치)   bool 함수 + 계단함수
adalin : bool 함수 + 활성화함수 + 계단함수


n
np 활성화함수 exists, 계단함수
nn 활성화함수 exists, 시그모이드, 이산 정수 0,1
    Ai 겨울
    nn 단위 -> 복수형 nnn (x)
-> nn 을 layer 라고 부르고 있습니다.
-> layer 가 1개
-> dnn (deep) { algorithm + nn + dframe + frunction(main) } -> model
cnn 레루, 연속 실수
runnablelstm
gan

dimension
예) set, tuple(list)


하이퍼 파라미터
    단순 파라미터는 값 한번 주고받음
    하이퍼 파라미터는 한번 전달하고 은닉층 또 전달하고 또 전달하고 ... 